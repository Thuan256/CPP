#include <bits/stdc++.h>
using namespace std;
int main()
{
    ifstream inp("STR.INP");
    ofstream out("STR.OUT");
    string n;
    inp >> n;
    vector<int> A;
    vector<int> B;
    for(auto i: n)
    {
        A.push_back(i);
    }
    for(int i=0;i<=A.size)
    {
        if(isdigit(i)==true)
            B.push_back(i);
        else
            if(i==0)
    }
}











