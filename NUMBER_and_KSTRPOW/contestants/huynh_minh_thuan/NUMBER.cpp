#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
    ifstream inp("NUMBER.inp");
    ofstream out("NUMBER.out");

    int n, k;
    inp >> n >> k;

    vector<ll> a(n);

    for (size_t i = 0; i < n; i++) inp >> a[i];
}
