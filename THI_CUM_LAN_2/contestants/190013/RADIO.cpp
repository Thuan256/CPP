#include <bits/stdc++.h>
using namespace std;
int main()
{
    ifstream inp("RADIO.INP");
    ofstream out("RADIO.OUT");
    int n;
    inp >> n;
    string k;
    inp >> k;
    int so=n/2;
    while(so--)
        {for(int i=0;i<n/2;i++)
        {
            string a;
            for(int j=0;j<so;i++)
            a.push_back(n[i])

        }}
        find A(3);
}
