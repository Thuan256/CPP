#include <bits/stdc++.h>
using namespace std;
int main()
{
    ifstream inp("GAME.INP");
    ofstream out("GAME.OUT");
    int n; B;
    ham(0,B);

    inp >> n;
    vector<int> A;
    while(n--)
    {
        int num;
        inp >> num;
        A.push_back(num);
        num.clear();
    }
    vector<int>
}
