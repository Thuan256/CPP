#include <iostream>
#include <vector>
#include <fstream>
#include <sstream>

using namespace std;

int main() {

    ifstream inp("LIS.inp");
    string line;
    
    getline(inp, line);
    short n = stoi(line);

    stringstream ss(line);

    vector


    return 0;
}